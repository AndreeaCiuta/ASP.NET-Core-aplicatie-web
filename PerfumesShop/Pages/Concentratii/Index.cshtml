@page
@model PerfumesShop.Pages.Concentratii.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Concentratii parfumuri</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Concentratie[0].Conctr)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Concentratie) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Conctr)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
